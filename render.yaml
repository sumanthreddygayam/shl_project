services:
  - type: web
    name: shl-recommender-api
    runtime: docker
    plan: free
    branch: main
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: SEMANTIC_RETRIEVAL_ENABLED
        value: "false"
